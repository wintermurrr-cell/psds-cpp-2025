# Найти все элементы

Необходимо реализовать функцию `FindAll`, которая принимает на вход контейнер
`std::vector<int>` и указатель на функцию предикат, и возвращает контейнер 
позиций всех элементов, удовлетворяющих предикату. 

## Примечание

- Запрещено использовать функции из `<algorithm>`